replicaCount: 1
namespace: pre-drivethru-agent-front
service:
  type: ClusterIP
  port: 80
  targetPort: 8080
image:
  pullPolicy: Always
extraEnv:
- name: "TZ"
  value: "UTC"
extraEnvFrom: []
resources:
  requests:
    memory: "16Mi"
  limits:
    memory: "32Mi"
ingress:
  enabled: true
  annotations:
    nginx.ingress.kubernetes.io/proxy-set-header: X-Real-IP $remote_addr
    nginx.ingress.kubernetes.io/use-forwarded-headers: "true"
  class: "nginx-local"
  host: "drivethru.pre.danielbeltejar.es"
  tls: true
config:
  routes:
  - pattern: "/api/menu"
    method:
    - "GET"
    backend_dns: "http://back-service.pre-drivethru-agent-back.svc.cluster.local:80"
  - pattern: "/api/chat"
    method:
    - "GET"
    - "POST"
    backend_dns: "http://back-service.pre-drivethru-agent-back.svc.cluster.local:80"
